services:
    _defaults:
        autowire: true
        autoconfigure: true

    BrightCloudStudio\RepFinderBundle\Controller\Backend\LocationsController:
        arguments:
            $db: '@doctrine.dbal.default_connection'
        public: true

    BrightCloudStudio\RepFinderBundle\Controller\FrontendModule\LocationsListController:
        # Tag is handled automatically by the #[AsFrontendModule] attribute
        # via autoconfigure above — no manual tag needed here
